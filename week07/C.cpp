#include <bits/stdc++.h>
using namespace std;

void solve() {
    int N;
    scanf("%d", &N);

    vector<int> a(N);
    for (int i = 0; i < N; i++) 
        scanf("%d", &a[i]);

}


int main() {
    int t;
    scanf("%d", &t);

    while (t--)
        solve();
}